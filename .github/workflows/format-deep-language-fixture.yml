name: Format deep language fixture
on:
  push:
    branches:
      - agent/mizan-monetization-premium
    paths:
      - .github/workflows/format-deep-language-fixture.yml
permissions:
  contents: write
jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/mizan-monetization-premium
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
      - run: dart format test/all_29_language_deep_surface_test.dart
      - run: rm .github/workflows/format-deep-language-fixture.yml
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'Canonical format 29-language legal fixture'
          git push origin HEAD:agent/mizan-monetization-premium
