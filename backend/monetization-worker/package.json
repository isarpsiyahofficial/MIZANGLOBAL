{
  "name": "mizan-monetization-worker",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "check": "npm run types && tsc --noEmit && wrangler deploy --dry-run --env production",
    "types": "wrangler types ./src/worker-configuration.d.ts --env production",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.115.0"
  }
}
